#include<bits/stdc++.h>
using namespace std;

int main(){
    ios::sync_with_stdio(0);        // or ios_base::sync_with_stdio(0)?
    cin.tie(0);                     // also instead of 0, can type false or NULL or nullptr
    cout.tie(0);

    /* in 2d arrays, uninitialized elems have = 0 
        in all traversal algs, use visited array -- like bfs and dfs    
    */

   // online compilers error if bfs vector used inside bfs function, but extra functions can be in private/public doesnt matter

//    vector<int> arr[3];
//    arr[0].push_back(4);
//    cout << arr[0][0];           // array of vectors also accessed like 2d array/vector



    return 0;
}